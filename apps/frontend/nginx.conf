server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Canvas fonts are served from /excalidraw/fonts/; gstatic serves the UI fonts linked in index.html.
    # Locations with their own add_header drop this, which is fine: only `location /` serves HTML.
    add_header Content-Security-Policy "font-src 'self' https://fonts.gstatic.com" always;

    # Vite hashed assets — immutable cache
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Other static files
    location ~* \.(ico|svg|png|jpg|jpeg|gif|woff2?|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public";
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;
}
